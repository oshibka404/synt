import 'package:flutter/material.dart';

/// Settings screen
class Controls extends StatelessWidget {
  // TODO: scale, tempo, key,
  @override
  Widget build(context) {
    return Center(
      child: Text("Settings"),
    );
  }
}
